---
description: Rank the board into a dispatch plan for one or more threads
argument-hint: [threads=N] [#parent|track:name] [ad-hoc work]
---

Use the `triage` skill.

Scope: $ARGUMENTS (`threads=N` for how many threads, `#N` or `track:name` to
narrow what gets ranked, and any ad-hoc work not yet filed; empty plans two
threads over the whole board)
